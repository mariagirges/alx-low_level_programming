#include <stdio.h>
#include "main.h"
/**
 * main - multiplies two numbers.
 * @argc: count arguments
 * @argv: Array Name
 * Return: 0
 */
int main(int argc, char *argv[])
{
	int a;

	if (argc == 1)
		printf("%d\n", argc - 1);
	else 
	{
		for (a = 0; *argv; i++, argv++)
			;

		printf("%d\n", a - 1);
	}
	return (0);
}
